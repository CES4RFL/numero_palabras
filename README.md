Antes de ejecutar por favor instalar la libreria numlet

Esta es la referencia a la documentacion de la libreria https://pypi.org/project/numlet/

Comando para instalar numlet:

pip install numlet

Ejecutar con el comando:

py main.py